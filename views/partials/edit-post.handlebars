<div class="mx-auto card border-dark mt-3 mb-0 edit-post" data-user-id="{{this.user.id}}" id="edit-form" hidden="true">
        <form class="card-body pt-0 pe-0">
            <div class="d-flex justify-content-end">
                <p class="fst-italic pe-1 text-black-50">{{format_date this.created_at}}</p>
            </div>
            <div class="m-0 p-0 me-3">
                <input type="text" class="my-2" name="edit-post-title" id = "edit-post-title" value="{{this.title}}"></input>
                <textarea class="my-2" name="edit-post-content" id= "edit-post-content" rows="4">{{this.coconut}}</textarea>
            </div>
            <div class="row">
            <div class="col-12 text-end">
                <p class="mb-0 pe-2">❤️{{this.upvote_count}} coco-ros | 👎{{this.downvote_count}} </p>
            </div> 
            </div>
        </form>
</div>